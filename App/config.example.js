// Copy this file to config.js and fill in your Supabase project values.
// config.js is gitignored — never commit real credentials.
// For GitHub Pages: add SUPABASE_URL and SUPABASE_ANON_KEY as repo secrets
// (Settings → Secrets and variables → Actions), then the deploy workflow injects them automatically.

const SUPABASE_URL = 'https://YOUR_PROJECT_ID.supabase.co';
const SUPABASE_ANON_KEY = 'YOUR_ANON_KEY_HERE';
